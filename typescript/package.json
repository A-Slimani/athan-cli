{
  "scripts": {
    "build": "tsup main.ts --format cjs --out-dir dist",
    "esbuild": "esbuild main.ts --bundle --platform=node --outfile=dist/main.js",
    "start": "node dist/main.js"
  },
  "pkg": {
    "targets": []
  },
  "dependencies": {
    "@types/axios": "^0.9.36",
    "@types/node": "^22.10.1",
    "axios": "^1.7.9",
    "table": "^6.9.0",
    "tsup": "^8.3.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
